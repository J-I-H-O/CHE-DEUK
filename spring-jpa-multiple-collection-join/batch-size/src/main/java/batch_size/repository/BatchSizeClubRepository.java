package batch_size.repository;

import batch_size.domain.BatchSizeClub;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BatchSizeClubRepository extends JpaRepository<BatchSizeClub, Long> {
}
